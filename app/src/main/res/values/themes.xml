<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.BrainSqueezer" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/status_bar_color</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.BrainSqueezer.NoActionBar" parent="Theme.BrainSqueezer">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="statusBarBackground">@color/status_bar_color</item>

    </style>


    <style name="TransparentSystemBars" parent="Theme.BrainSqueezer.NoActionBar">
        <!--        <item name="android:navigationBarColor">-->
        <!--            @color/white-->
        <!--        </item>-->

        <!-- Optional, if drawing behind the status bar too -->
        <item name="android:statusBarColor">
            @android:color/transparent
        </item>
    </style>

    <style name="Theme.BrainSqueezer.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.BrainSqueezer.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Theme.BrainSqueezer.NoActionBar.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.BrainSqueezer.NoActionBar.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.BrainSqueezer.NoActionBar.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <color name="status_bar_color">#ff0077B6</color>
</resources>